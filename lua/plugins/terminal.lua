return {
  'akinsho/toggleterm.nvim',
  version = "*",
  opts = {
    -- The key you want to use to toggle the terminal
    open_mapping = [[<c-\>]], 
    direction = 'horizontal', -- Can be 'horizontal', 'vertical', or 'float'
    size = 15,
  }
}
